<resources>
    <!--
      Pre-Compose window theme, shown during activity launch before setContent
      runs. App is dark-only (see HeartbeatTheme in ui/theme) so this uses the
      base android:Theme.Material, which is dark, not the Light/DayNight variant.
    -->
    <style name="Theme.Heartbeat" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
