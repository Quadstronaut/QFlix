package com.qflix.heartbeat.ui.theme

import androidx.compose.material3.Typography

// Material 3 default type scale. Custom fonts can slot in later without
// touching call sites — every screen reads sizes off MaterialTheme.typography.
val Typography = Typography()
